services:
  navidrome:
    image: deluan/navidrome:latest
    user: 1000:1000 # should be owner of volumes
    ports:
      - "4533:4533"
    restart: unless-stopped
    volumes:
      - "/mnt/media/docker-vol/navi/data:/data"
      - "/mnt/media/docker-vol/navi/music:/music:ro"
